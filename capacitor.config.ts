import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: "com.otaku254.app",
  appName: "Otaku254",
  webDir: "dist",
  server: {
    androidScheme: "https",
  },
};

export default config;
