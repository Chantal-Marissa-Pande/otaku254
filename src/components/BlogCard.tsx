import { Link } from "react-router-dom";
interface Props { id: string; title: string; description: string; image?: string; category: string; readTime?: string; author?: string; }
export default function BlogCard({ id, title, description, image, category, readTime, author }: Props) {
  return <Link to={`/post/${id}`} className="story-card group">{image ? <div className="relative h-52 overflow-hidden"><img src={image} alt={title} className="h-full w-full object-cover transition duration-500 group-hover:scale-105" /><span className="category-badge">{category}</span></div> : <div className="card-placeholder"><span className="category-badge">{category}</span><span>✦</span></div>}<div className="p-5"><div className="mb-3 flex items-center justify-between text-xs font-semibold text-[var(--otaku-accent)]"><span>{author ? `By ${author}` : "Otaku254"}</span>{readTime && <span className="text-[var(--otaku-muted)]">{readTime}</span>}</div><h3 className="text-xl font-bold leading-snug tracking-tight group-hover:text-[var(--otaku-accent)]">{title}</h3><p className="mt-3 line-clamp-3 text-sm leading-6 text-[var(--otaku-muted)]">{description}</p><span className="mt-5 inline-flex items-center gap-2 text-sm font-bold text-[var(--otaku-accent)]">Read story <span aria-hidden="true">→</span></span></div></Link>;
}
